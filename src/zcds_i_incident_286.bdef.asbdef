interface;
use draft;

define behavior for ZCDS_I_INCIDENT_286 alias Incident
{
  use create;
  use update;
  use delete;

  use action NewStatus;
  use action validateStatusCode;
  use action validatePriorityCode;
  use action validateDates;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}