projection;
strict ( 2 );
use draft;

define behavior for ZCDS_C_INCIDENT_286 //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action NewStatus;
  use action validateStatusCode;
  use action validatePriorityCode;
  use action validateDates;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}