projection;
strict ( 2 );
use draft;

define behavior for ZCDS_C_INCIDENT_286 alias Incident
{
  use create;
  use update;
  use delete;

  use action NewStatus;
  use action validaCamposMandatorios;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;

  use association _History { create; with draft; }
}

define behavior for zcds_c_history_286 alias History
use etag
{
  use association _Incident { with draft; }
}